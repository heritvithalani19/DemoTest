import { StyleSheet, Platform } from 'react-native';
import { Colors, Matrics, Fonts, MainStyles } from '../../CommonConfig';

export const loginStyle = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: Colors.PRIMARY
    }
})